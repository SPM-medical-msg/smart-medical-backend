<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.xxx.modules.mapper.PlanMapper">

<select id="selectListInfo" resultType="com.xxx.modules.entity.Plan">
select `p`.*,u.real_name from `plan` p   left join `user` u on p.user_id = u.id
   where 1=1
    <if test="userId !=null">
        and p.user_id = #{userId}
    </if>
    <if test="type !=null">
        and p.type = #{type}
    </if>
    <if test="time !=null">
        and p.time = #{time}
    </if>
    <if test="realName !=null">
        and u.real_name like concat('%',#{realName},'%')
    </if>
        </select>


</mapper>